# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: custom-cicd-test # Choose a name for your app
spec:
  replicas: 2 # Run 2 copies for reliability
  selector:
    matchLabels:
      app: custom-cicd-test
  template:
    metadata:
      labels:
        app: custom-cicd-test
    spec:
      containers:
      - name: app
        # !! THIS LINE GETS UPDATED BY GITHUB ACTIONS !!
        image: ghcr.io/aeskerminen/custom-cicd-test:2ddd52d4ee14b27bc36e46086b40db28ada2944d
        ports:
        - containerPort: 80 # The port your app listens on INSIDE the container
